VITE_API_URL=http://localhost:5000/api

# Google OAuth Client ID (same value as backend's GOOGLE_CLIENT_ID) - this
# one is public by design, Google's Identity Services SDK expects it
# client-side. Leave blank to hide the "Continue with Google" button.
VITE_GOOGLE_CLIENT_ID=
